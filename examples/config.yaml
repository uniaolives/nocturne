# config.yaml –
sdk:
  log_level: INFO
  cache_dir: "~/.nocturne/sdk_cache"

trauma_engine:
  endpoint: "https://api.nocturne.io/trauma"
  api_key: "dummy-key"

forget_machine:
  policy_file: "policies/default.yaml"
  proof_store: "./proofs"

mirror_network:
  bootstrap_peers:
    - "/ip4/10.0.0.5/tcp/4001/p2p/QmBootstrapNode"
  max_peers: 50

scroll_publisher:
  rpc_url: "https://rpc.scroll.io"
  contract_address: "0x1234567890abcdef1234567890abcdef12345678"
  wallet_path: "~/.nocturne/wallet.json"

dam:
  backend: "ipfs"
  ipfs_api: "http://127.0.0.1:5001"
